using DG.Tweening;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine;

public class testet : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Sequence seq = DOTween.Sequence();
        //seq.Append(transform.DOLocalMoveX(10, 2f).SetRelative())
        //    .OnComplete(() => Debug.Log("AHOY"));
        //seq.Complete();
          
        //gameObject.transform.DOLocalMoveX(10, 2f).SetRelative().OnComplete(() => Debug.Log("AHOY"));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
